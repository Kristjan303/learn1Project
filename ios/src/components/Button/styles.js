import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#4f63ac',
        paddingVertical: 20,
        paddingHorizontal: 8,
        width: "100%",
        borderRadius: 10
    },
    title: {
        color: "#fff",
        textAlign: "center",
        fontSize: 16,
        fontWeight: "bold"
    }
})